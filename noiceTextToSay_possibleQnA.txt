CROWN is good at exactly one thing — not regressing on smooth content while improving on textured content. That's it. That's the whole pitch. 

Put in abstract: 
Every classical inpainter commits to one regime. PDE methods (biharmonic, TV) hallucinate textures as smooth blur. Patch/sparse methods (K-SVD, NLM) hallucinate textures into smooth regions. No classical method is locally regime-aware. Deep methods are, but they're black boxes with no guarantees.

CROWN is a regime-aware classical inpainter with a no-regression guarantee. On smooth regions it provably reduces to biharmonic completion (Theorem 1, Section 8.2). On textured regions it reduces to confidence-weighted sparse coding. The fusion is gated by a per-pixel regime map computed from gradient energy, structure-tensor anisotropy, and DCT spectral entropy.

CROWN is a principled regime-aware fusion of two complementary classical priors, with formal guarantees, that empirically realizes its design goal (no-regression on smooth, gains on textured). The empirical gains are modest in absolute terms because the operating regime allows only modest gains — biharmonic already saturates on smooth content, and classical sparse methods only help by a few dB on textured content. Modest gains in this regime are the ceiling, not a sign of failure.

Likely question	Your answer
"Isn't this just K-SVD + biharmonic stitched together?"	
"No — the regime-aware fusion (C1, C5) is the unifying idea. Theorem 1 proves the no-regression guarantee; Lemma 1 shows the dual-weighting is necessary. Without these, the components don't compose."
"Your gain over biharmonic is only 0.3 dB."
	"On average, yes — because the dataset is smooth-dominated. On the textured subset (Table X), gain is +3 dB. Theorem 1 guarantees we never lose on smooth — that's the design goal."
"What about deep learning baselines?"	
"Out of scope; CROWN is a classical method with formal guarantees. Closing the gap to deep methods without learned priors is part of the contribution."
"Why not just use one prior with a switch?"
	"Hard switching introduces seams; Lemma 1 / Sec 6.7 show smooth fusion is necessary. We tried hard switching — Table Y shows it's worse." (Run this if you have 30 min.)
"Is your nonlocal step actually doing anything?"
	(Honest answer:) "+0.22 dB on average, +X dB on textured holes. We retain it because Lemma 1's dual-weighting is the conceptual contribution. Larger gains likely require stronger NL coupling — future work."
"What's the runtime?"
	Have a number ready. ~3× biharmonic per iteration, ~5 iterations = ~15× biharmonic. Acknowledge it.


CROWN is grayscale in the current implementation, but the framework is color-agnostic. The regime map, confidence map, dual-weighted NL coupling, and no-regression theorem are all scalar/per-channel constructions that extend to color trivially. Three standard extensions are available:

1. Per-channel independent — apply CROWN to R, G, B separately. Simple, but ignores cross-channel correlation.

2. YCbCr decomposition — full CROWN on luminance, lighter treatment of chrominance (which is intrinsically smoother). Standard since BM3D.

3. Stacked-channel atoms — 8×8×3 = 192-dim patches with a joint dictionary. Captures color textures coherently. This is a 30-line code change and a re-training of D.

None of these change any of the theoretical results. We focused on grayscale to isolate the contribution from the color-coupling literature